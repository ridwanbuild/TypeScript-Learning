// enum




enum TrafficLight {
    Red,
    Yellow,
    Green
}

enum Role {
    Admin,
    SuperAdmin
}

// if (Role.Admin === "Admin") {

// }


let signal: TrafficLight = TrafficLight.Green
console.log(signal);
